# NOTE: This file is generated by `./scripts/render_main_tf.sh`.
#       Please Read `./scripts/render_main_tf.sh` for more information.

provider "aws" {
    region = "ap-northeast-1"
    alias  = "ap-northeast-1"
    access_key = "${var.access_key}"
    secret_key = "${var.secret_key}"
}

resource "aws_vpc" "vpc-ap-northeast-1" {
  provider      = aws.ap-northeast-1
  cidr_block           = "172.31.0.0/16"
  enable_dns_hostnames = true
  tags = {
    Name = "${var.prefix}-vpc"
  }
}

resource "aws_subnet" "subnet-ap-northeast-1" {
  provider      = aws.ap-northeast-1
  vpc_id                  = aws_vpc.vpc-ap-northeast-1.id
  cidr_block           = "172.31.0.0/16"
  map_public_ip_on_launch = true
  tags = {
    Name = "${var.prefix}-subnet"
  }
}

resource "aws_internet_gateway" "ig-ap-northeast-1" {
  provider      = aws.ap-northeast-1
  vpc_id                  = aws_vpc.vpc-ap-northeast-1.id
  tags = {
    Name = "${var.prefix}"
  }
}

resource "aws_route_table" "rt-ap-northeast-1" {
  provider      = aws.ap-northeast-1
  vpc_id                  = aws_vpc.vpc-ap-northeast-1.id
  route {
      cidr_block = "0.0.0.0/0"
      gateway_id = "${aws_internet_gateway.ig-ap-northeast-1.id}"
    }
  tags = {
    Name = "${var.prefix}"
  }
}

resource "aws_route_table_association" "subnet-association-ap-northeast-1" {
  provider      = aws.ap-northeast-1
  subnet_id      = "${aws_subnet.subnet-ap-northeast-1.id}"
  route_table_id = "${aws_route_table.rt-ap-northeast-1.id}"
}

data "aws_ami" "ubuntu-ap-northeast-1" {
  provider = aws.ap-northeast-1
  most_recent = true
  owners      = ["099720109477"] # Canonical
  filter {
    name   = "name"
    values = ["ubuntu/images/hvm-ssd/ubuntu-focal-20.04-amd64-server-*"]
  }
}

resource "aws_key_pair" "key-ap-northeast-1" {
  provider      = aws.ap-northeast-1
  key_name   = "${var.prefix}"
  public_key = file(var.public_key_path)
}

resource "aws_security_group" "sg-ap-northeast-1" {
  provider      = aws.ap-northeast-1
  name   = "${var.prefix}-sg"
  vpc_id = aws_vpc.vpc-ap-northeast-1.id

  ingress {
    description = "ssh"
    from_port   = 22
    to_port     = 22
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }

  ingress {
    description = "all inbound from anywhere"
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }

  egress {
    description = "all outbound to anywhere"
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }

  tags = {
    Name = "${var.prefix}"
  }
}

resource "aws_instance" "instance-ap-northeast-1" {
  provider = aws.ap-northeast-1
  ami           = data.aws_ami.ubuntu-ap-northeast-1.id
  key_name      = aws_key_pair.key-ap-northeast-1.id
  instance_type = "${var.instance_type}"
  vpc_security_group_ids = [aws_security_group.sg-ap-northeast-1.id]
  subnet_id              = aws_subnet.subnet-ap-northeast-1.id
  root_block_device {
    volume_size = "60"
  }
  tags = {
    Name  = "${var.prefix}"
  }

  provisioner "remote-exec" {
    inline = [
      "while [ ! -f /var/lib/cloud/instance/boot-finished ]; do echo 'Waiting for cloud-init...'; sleep 1; done",
    ]

    connection {
        type        = "ssh"
        host        = aws_instance.instance-ap-northeast-1.public_ip
        user        = var.username
        private_key = file(var.private_key_path)
    }
  }
}

output "ansible-hosts-ap-northeast-1" {
  value = <<EOF

ap-northeast-1:
  hosts:
    instance-ap-northeast-1:
      ansible_host: ${aws_instance.instance-ap-northeast-1.public_ip}
      instance_type: ${aws_instance.instance-ap-northeast-1.instance_type}
      region: ap-northeast-1
      prefix: ${var.prefix}
      ansible_user: ${var.username}
EOF
}
# NOTE: This file is generated by `./scripts/render_main_tf.sh`.
#       Please Read `./scripts/render_main_tf.sh` for more information.

provider "aws" {
    region = "ap-southeast-1"
    alias  = "ap-southeast-1"
    access_key = "${var.access_key}"
    secret_key = "${var.secret_key}"
}

resource "aws_vpc" "vpc-ap-southeast-1" {
  provider      = aws.ap-southeast-1
  cidr_block           = "172.31.0.0/16"
  enable_dns_hostnames = true
  tags = {
    Name = "${var.prefix}-vpc"
  }
}

resource "aws_subnet" "subnet-ap-southeast-1" {
  provider      = aws.ap-southeast-1
  vpc_id                  = aws_vpc.vpc-ap-southeast-1.id
  cidr_block           = "172.31.0.0/16"
  map_public_ip_on_launch = true
  tags = {
    Name = "${var.prefix}-subnet"
  }
}

resource "aws_internet_gateway" "ig-ap-southeast-1" {
  provider      = aws.ap-southeast-1
  vpc_id                  = aws_vpc.vpc-ap-southeast-1.id
  tags = {
    Name = "${var.prefix}"
  }
}

resource "aws_route_table" "rt-ap-southeast-1" {
  provider      = aws.ap-southeast-1
  vpc_id                  = aws_vpc.vpc-ap-southeast-1.id
  route {
      cidr_block = "0.0.0.0/0"
      gateway_id = "${aws_internet_gateway.ig-ap-southeast-1.id}"
    }
  tags = {
    Name = "${var.prefix}"
  }
}

resource "aws_route_table_association" "subnet-association-ap-southeast-1" {
  provider      = aws.ap-southeast-1
  subnet_id      = "${aws_subnet.subnet-ap-southeast-1.id}"
  route_table_id = "${aws_route_table.rt-ap-southeast-1.id}"
}

data "aws_ami" "ubuntu-ap-southeast-1" {
  provider = aws.ap-southeast-1
  most_recent = true
  owners      = ["099720109477"] # Canonical
  filter {
    name   = "name"
    values = ["ubuntu/images/hvm-ssd/ubuntu-focal-20.04-amd64-server-*"]
  }
}

resource "aws_key_pair" "key-ap-southeast-1" {
  provider      = aws.ap-southeast-1
  key_name   = "${var.prefix}"
  public_key = file(var.public_key_path)
}

resource "aws_security_group" "sg-ap-southeast-1" {
  provider      = aws.ap-southeast-1
  name   = "${var.prefix}-sg"
  vpc_id = aws_vpc.vpc-ap-southeast-1.id

  ingress {
    description = "ssh"
    from_port   = 22
    to_port     = 22
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }

  ingress {
    description = "all inbound from anywhere"
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }

  egress {
    description = "all outbound to anywhere"
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }

  tags = {
    Name = "${var.prefix}"
  }
}

resource "aws_instance" "instance-ap-southeast-1" {
  provider = aws.ap-southeast-1
  ami           = data.aws_ami.ubuntu-ap-southeast-1.id
  key_name      = aws_key_pair.key-ap-southeast-1.id
  instance_type = "${var.instance_type}"
  vpc_security_group_ids = [aws_security_group.sg-ap-southeast-1.id]
  subnet_id              = aws_subnet.subnet-ap-southeast-1.id
  root_block_device {
    volume_size = "60"
  }
  tags = {
    Name  = "${var.prefix}"
  }

  provisioner "remote-exec" {
    inline = [
      "while [ ! -f /var/lib/cloud/instance/boot-finished ]; do echo 'Waiting for cloud-init...'; sleep 1; done",
    ]

    connection {
        type        = "ssh"
        host        = aws_instance.instance-ap-southeast-1.public_ip
        user        = var.username
        private_key = file(var.private_key_path)
    }
  }
}

output "ansible-hosts-ap-southeast-1" {
  value = <<EOF

ap-southeast-1:
  hosts:
    instance-ap-southeast-1:
      ansible_host: ${aws_instance.instance-ap-southeast-1.public_ip}
      instance_type: ${aws_instance.instance-ap-southeast-1.instance_type}
      region: ap-southeast-1
      prefix: ${var.prefix}
      ansible_user: ${var.username}
EOF
}
# NOTE: This file is generated by `./scripts/render_main_tf.sh`.
#       Please Read `./scripts/render_main_tf.sh` for more information.

provider "aws" {
    region = "us-east-1"
    alias  = "us-east-1"
    access_key = "${var.access_key}"
    secret_key = "${var.secret_key}"
}

resource "aws_vpc" "vpc-us-east-1" {
  provider      = aws.us-east-1
  cidr_block           = "172.31.0.0/16"
  enable_dns_hostnames = true
  tags = {
    Name = "${var.prefix}-vpc"
  }
}

resource "aws_subnet" "subnet-us-east-1" {
  provider      = aws.us-east-1
  vpc_id                  = aws_vpc.vpc-us-east-1.id
  cidr_block           = "172.31.0.0/16"
  map_public_ip_on_launch = true
  tags = {
    Name = "${var.prefix}-subnet"
  }
}

resource "aws_internet_gateway" "ig-us-east-1" {
  provider      = aws.us-east-1
  vpc_id                  = aws_vpc.vpc-us-east-1.id
  tags = {
    Name = "${var.prefix}"
  }
}

resource "aws_route_table" "rt-us-east-1" {
  provider      = aws.us-east-1
  vpc_id                  = aws_vpc.vpc-us-east-1.id
  route {
      cidr_block = "0.0.0.0/0"
      gateway_id = "${aws_internet_gateway.ig-us-east-1.id}"
    }
  tags = {
    Name = "${var.prefix}"
  }
}

resource "aws_route_table_association" "subnet-association-us-east-1" {
  provider      = aws.us-east-1
  subnet_id      = "${aws_subnet.subnet-us-east-1.id}"
  route_table_id = "${aws_route_table.rt-us-east-1.id}"
}

data "aws_ami" "ubuntu-us-east-1" {
  provider = aws.us-east-1
  most_recent = true
  owners      = ["099720109477"] # Canonical
  filter {
    name   = "name"
    values = ["ubuntu/images/hvm-ssd/ubuntu-focal-20.04-amd64-server-*"]
  }
}

resource "aws_key_pair" "key-us-east-1" {
  provider      = aws.us-east-1
  key_name   = "${var.prefix}"
  public_key = file(var.public_key_path)
}

resource "aws_security_group" "sg-us-east-1" {
  provider      = aws.us-east-1
  name   = "${var.prefix}-sg"
  vpc_id = aws_vpc.vpc-us-east-1.id

  ingress {
    description = "ssh"
    from_port   = 22
    to_port     = 22
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }

  ingress {
    description = "all inbound from anywhere"
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }

  egress {
    description = "all outbound to anywhere"
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }

  tags = {
    Name = "${var.prefix}"
  }
}

resource "aws_instance" "instance-us-east-1" {
  provider = aws.us-east-1
  ami           = data.aws_ami.ubuntu-us-east-1.id
  key_name      = aws_key_pair.key-us-east-1.id
  instance_type = "${var.instance_type}"
  vpc_security_group_ids = [aws_security_group.sg-us-east-1.id]
  subnet_id              = aws_subnet.subnet-us-east-1.id
  root_block_device {
    volume_size = "60"
  }
  tags = {
    Name  = "${var.prefix}"
  }

  provisioner "remote-exec" {
    inline = [
      "while [ ! -f /var/lib/cloud/instance/boot-finished ]; do echo 'Waiting for cloud-init...'; sleep 1; done",
    ]

    connection {
        type        = "ssh"
        host        = aws_instance.instance-us-east-1.public_ip
        user        = var.username
        private_key = file(var.private_key_path)
    }
  }
}

output "ansible-hosts-us-east-1" {
  value = <<EOF

us-east-1:
  hosts:
    instance-us-east-1:
      ansible_host: ${aws_instance.instance-us-east-1.public_ip}
      instance_type: ${aws_instance.instance-us-east-1.instance_type}
      region: us-east-1
      prefix: ${var.prefix}
      ansible_user: ${var.username}
EOF
}
# NOTE: This file is generated by `./scripts/render_main_tf.sh`.
#       Please Read `./scripts/render_main_tf.sh` for more information.

provider "aws" {
    region = "us-west-1"
    alias  = "us-west-1"
    access_key = "${var.access_key}"
    secret_key = "${var.secret_key}"
}

resource "aws_vpc" "vpc-us-west-1" {
  provider      = aws.us-west-1
  cidr_block           = "172.31.0.0/16"
  enable_dns_hostnames = true
  tags = {
    Name = "${var.prefix}-vpc"
  }
}

resource "aws_subnet" "subnet-us-west-1" {
  provider      = aws.us-west-1
  vpc_id                  = aws_vpc.vpc-us-west-1.id
  cidr_block           = "172.31.0.0/16"
  map_public_ip_on_launch = true
  tags = {
    Name = "${var.prefix}-subnet"
  }
}

resource "aws_internet_gateway" "ig-us-west-1" {
  provider      = aws.us-west-1
  vpc_id                  = aws_vpc.vpc-us-west-1.id
  tags = {
    Name = "${var.prefix}"
  }
}

resource "aws_route_table" "rt-us-west-1" {
  provider      = aws.us-west-1
  vpc_id                  = aws_vpc.vpc-us-west-1.id
  route {
      cidr_block = "0.0.0.0/0"
      gateway_id = "${aws_internet_gateway.ig-us-west-1.id}"
    }
  tags = {
    Name = "${var.prefix}"
  }
}

resource "aws_route_table_association" "subnet-association-us-west-1" {
  provider      = aws.us-west-1
  subnet_id      = "${aws_subnet.subnet-us-west-1.id}"
  route_table_id = "${aws_route_table.rt-us-west-1.id}"
}

data "aws_ami" "ubuntu-us-west-1" {
  provider = aws.us-west-1
  most_recent = true
  owners      = ["099720109477"] # Canonical
  filter {
    name   = "name"
    values = ["ubuntu/images/hvm-ssd/ubuntu-focal-20.04-amd64-server-*"]
  }
}

resource "aws_key_pair" "key-us-west-1" {
  provider      = aws.us-west-1
  key_name   = "${var.prefix}"
  public_key = file(var.public_key_path)
}

resource "aws_security_group" "sg-us-west-1" {
  provider      = aws.us-west-1
  name   = "${var.prefix}-sg"
  vpc_id = aws_vpc.vpc-us-west-1.id

  ingress {
    description = "ssh"
    from_port   = 22
    to_port     = 22
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }

  ingress {
    description = "all inbound from anywhere"
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }

  egress {
    description = "all outbound to anywhere"
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }

  tags = {
    Name = "${var.prefix}"
  }
}

resource "aws_instance" "instance-us-west-1" {
  provider = aws.us-west-1
  ami           = data.aws_ami.ubuntu-us-west-1.id
  key_name      = aws_key_pair.key-us-west-1.id
  instance_type = "${var.instance_type}"
  vpc_security_group_ids = [aws_security_group.sg-us-west-1.id]
  subnet_id              = aws_subnet.subnet-us-west-1.id
  root_block_device {
    volume_size = "60"
  }
  tags = {
    Name  = "${var.prefix}"
  }

  provisioner "remote-exec" {
    inline = [
      "while [ ! -f /var/lib/cloud/instance/boot-finished ]; do echo 'Waiting for cloud-init...'; sleep 1; done",
    ]

    connection {
        type        = "ssh"
        host        = aws_instance.instance-us-west-1.public_ip
        user        = var.username
        private_key = file(var.private_key_path)
    }
  }
}

output "ansible-hosts-us-west-1" {
  value = <<EOF

us-west-1:
  hosts:
    instance-us-west-1:
      ansible_host: ${aws_instance.instance-us-west-1.public_ip}
      instance_type: ${aws_instance.instance-us-west-1.instance_type}
      region: us-west-1
      prefix: ${var.prefix}
      ansible_user: ${var.username}
EOF
}
